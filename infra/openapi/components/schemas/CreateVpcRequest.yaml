CreateVpcRequest:
  type: object
  required: [name, cidr]
  properties:
    name:
      type: string
    cidr:
      type: string
      example: 10.0.0.0/16
    subnets:
      type: array
      items:
        $ref: "#/components/schemas/SubnetDefinition"
